# Copy to config.yaml and fill secrets
search_url: "https://www.city24.lv/your-search-url-with-filters"
telegram:
  bot_token: "123456789:ABCDEF..."
  chat_id: "987654321"       # your chat id (user or group)
filters:
  min_price: 0
  max_price: 300000
  regions:                     # optional: list of human-readable regions (for message)
    - "Rīga"
    - "Jūrmala"
message:
  max_per_run: 10              # max new notifications per run
